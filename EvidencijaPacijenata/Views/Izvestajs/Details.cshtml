@model EvidencijaPacijenata.Models.Izvestaj

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Izvestaj</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DatumPregleda)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DatumPregleda)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Dijagnoza)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Dijagnoza)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Korisnik)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Korisnik.Ime) @Html.DisplayFor(model => model.Korisnik.Prezime)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Korisnik1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Korisnik1.Ime) @Html.DisplayFor(model => model.Korisnik1.Prezime)
        </dd>

    </dl>
</div>
<p>
    @if (Session["IDAdmina"] != null || (Session["IDLekara"] != null && Convert.ToInt32(Session["IDLekara"]) == Model.IDLekara))
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.ID })
        <text> |</text>
    }
    @if (Session["IDPacijenta"] != null || Session["IDAdmina"] != null || Session["IDLekara"] != null)
    {
        @Html.ActionLink("Back to List", "Index", new { id = Model.IDPacijenta })
    }
</p>
