@model IEnumerable<EvidencijaPacijenata.Models.Izvestaj>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2>Index</h2>
@if (Session["IDAdmina"] != null)
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DatumPregleda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Dijagnoza)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Korisnik)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Korisnik1)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DatumPregleda.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dijagnoza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Korisnik.Ime) @Html.DisplayFor(modelItem => item.Korisnik.Prezime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Korisnik1.Ime) @Html.DisplayFor(modelItem => item.Korisnik1.Prezime)
            </td>
            <td>
                @Html.ActionLink("Details", "Details", new { id = item.ID })
                @if (Session["IDAdmina"] != null || (Session["IDLekara"] != null && Convert.ToInt32(Session["IDLekara"]) == item.IDLekara))
                {
                    <text> | </text>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ID })<text> | </text>

                    @Html.ActionLink("Delete", "Delete", new { id = item.ID })
                }
            </td>
        </tr>
    }

</table>
